# CSS Variables

Practice with custom properties (CSS variables) and scoping.

## Concepts Covered
- Declaring variables on `:root`
- Reading variables with `var()`
- Overriding a variable in a narrower scope

## How to Run
Open `index.html` in a browser.
