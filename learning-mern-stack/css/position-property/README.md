# Position Property

Practice with every CSS `position` value in one page.

## Concepts Covered
- `static`, `relative`, `absolute`, `fixed`, `sticky`
- How `top` / `left` / `bottom` / `right` behave under each

## How to Run
Open `index.html` in a browser and scroll to see the sticky/fixed behavior.
