# Map & Filter with HTML Rendering

Practice rendering an array of objects (student records) into the DOM,
and filtering it live.

## Concepts Covered
- Arrays of objects
- `map()` for dynamic HTML rendering
- `filter()` for search/filter behavior
- Template literals

## How to Run
Open `index.html` in a browser.
