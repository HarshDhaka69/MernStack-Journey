# Data Types

Practice with JavaScript's dynamic typing and type conversion.

## Concepts Covered
- Dynamic typing (`typeof` before/after reassignment)
- `undefined` vs `null`
- Type coercion in arithmetic
- Explicit conversion: `String()`, `Number()`, `Boolean()`

## How to Run
Open `index.html` in a browser and check the console.
