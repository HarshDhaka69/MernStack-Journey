# HTML Forms

Practice with common form input types and basic validation attributes.

## Concepts Covered
- Text, number, email, date, and password inputs
- Radio button groups
- `minlength` / `maxlength` / `required` validation attributes
- `<label for>` association

## How to Run
Open `index.html` in a browser.
